language: cpp
compiler: gcc
dist: xenial

branches:
  only:
    - master

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-6

before_script:
  - CXX=/usr/bin/g++-6 CC=/usr/bin/gcc-6

script:
  - mkdir build && cd build
  - cmake ../
  - cmake --build .
  - ctest